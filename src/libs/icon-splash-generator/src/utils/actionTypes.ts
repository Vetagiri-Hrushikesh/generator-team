// actionTypes.ts

// Action type for setting the selected icon in the state
export const SET_ICON = 'SET_ICON';

// Action type for setting the background color or image in the state
export const SET_BACKGROUND = 'SET_BACKGROUND';

// Action type for setting the size of the icon in the state
export const SET_ICON_SIZE = 'SET_ICON_SIZE';

// Action type for toggling the authentication status in the state
export const SET_AUTHENTICATION = 'SET_AUTHENTICATION';

// Action type for setting the package type (e.g., basic or premium) in the state
export const SET_PACKAGE_TYPE = 'SET_PACKAGE_TYPE';

// Action type for setting the role of the user (e.g., user or admin) in the state
export const SET_ROLE = 'SET_ROLE';

// Action type for setting the features available to the user based on their package and role
export const SET_FEATURES = 'SET_FEATURES';

// Action type for setting the controls available to the user based on their package and role
export const SET_CONTROLS = 'SET_CONTROLS';
